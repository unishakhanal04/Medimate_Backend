// Note: Use types from user.type.ts instead. These DTOs are kept for backward compatibility only.
// RegisterDTO and LoginDTO are defined in user.type.ts

export type { RegisterDTO, LoginDTO } from "../types/user.type";
